# Title Case Command

Example MacPad Pro script plugin. It adds `Title Case Selection` to the text commands menu and transforms the selected text through `transform.js`.

The package manifest declares the script file, source URL, SHA-256 checksum, and text permissions.
